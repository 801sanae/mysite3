<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="board">
	
	<insert id="insert" parameterType="BoardVo">
		<![CDATA[
		INSERT INTO BOARD VALUES(
				BOARD_NO_SEQ.NEXTVAL,#{title},#{contents},#{member_no},#{view_cnt},SYSDATE)
		]]>
	</insert>
	
	<update id="update" parameterType="BoardVo">
		<![CDATA[
			UPDATE board SET title=#{title}, content=#{contents}
			WHERE no=#{no}
		]]>	
	</update>
	
	<select id="getView" resultType="BoardVo">
		<![CDATA[		
			SELECT no, title, content, member_no 
			FROM board 
			WHERE no=#{no}
		]]>			
	</select>
	
	<select id="getList" resultType="BoardVo">
		<![CDATA[
				SELECT a.no, a.title, a.member_no, b.name as member_name, a.view_cnt, 
						to_char(a.reg_date, 'yyyy-mm-dd hh:mi:ss') AS reg_date
				FROM board a, member b
				WHERE a.member_no = b.no
				ORDER BY a.reg_date desc
		]]>
	</select>
	
</mapper>
